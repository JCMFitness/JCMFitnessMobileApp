<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    
    x:Class="JCMFitnessMobileApp.Views.WorkoutDetailPage">


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Delete" Command="{Binding DeleteCommand}" />
    </ContentPage.ToolbarItems>


    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

           

            <BoxView Grid.Row="1" BackgroundColor="White"
                Opacity=".8" />
            <StackLayout Padding="10" Grid.Row="1">
                <Label x:Name="title" Text="{Binding Workout.Title}"
                    HorizontalOptions="Center" AutomationProperties.IsInAccessibleTree="true"
                       AutomationProperties.HelpText="Title of trip" />
            

                <Label x:Name="notes" Text="{Binding Workout.Description}" HorizontalOptions="Center"
                       AutomationProperties.IsInAccessibleTree="true"
                       AutomationProperties.HelpText="Notes from trip" />
                
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>